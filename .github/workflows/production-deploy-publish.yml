name: Deprecated — Use URAI Jobs Canonical Production Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment authority notice
        run: |
          echo "[BLOCKED] This legacy deployment workflow is disabled."
          echo "Use .github/workflows/urai-jobs-production-deploy.yml."
          echo "The canonical workflow requires an exact target SHA, rollback SHA, workload identity, Secret Manager bindings, and immutable receipts."
