{
  "name": "urai-jobs",
  "version": "1.0.0",
  "scripts": {
    "lint": "npm run lint -w functions",
    "build": "npm run build -w functions",
    "serve": "firebase emulators:start --only functions",
    "dev": "firebase emulators:start"
  },
  "workspaces": [
    "functions"
  ]
}