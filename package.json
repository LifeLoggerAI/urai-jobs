{
  "name": "urai-jobs-monorepo",
  "private": true,
  "scripts": {
    "install": "pnpm install",
    "test": "pnpm test -r",
    "typecheck": "pnpm typecheck -r",
    "lint": "pnpm lint -r",
    "build": "pnpm build -r",
    "deploy:emulators": "firebase emulators:start",
    "deploy:prod": "firebase deploy --only functions,firestore:rules,firestore:indexes"
  }
}
