{
  "name": "urai-jobs-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"pnpm --filter web dev\" \"firebase emulators:start\"",
    "build": "pnpm --filter '*' build",
    "deploy": "firebase deploy",
    "test": "pnpm --filter '*' test",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "devDependencies": {
    "firebase-tools": "^13.11.0",
    "concurrently": "^8.2.2"
  }
}
