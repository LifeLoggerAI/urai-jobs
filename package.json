{
  "name": "urai-jobs",
  "private": true,
  "workspaces": [
    "functions",
    "web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w web\" \"npm run emulators\"",
    "build": "npm run build -w web && npm run build -w functions",
    "emulators": "firebase emulators:start",
    "deploy": "firebase deploy",
    "seed": "node scripts/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
