{
  "name": "urai-jobs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"pnpm run emulators\" \"pnpm run dev --filter web\"",
    "build": "pnpm run build --filter web && pnpm run build --filter functions",
    "emulators": "firebase emulators:start",
    "seed": "ts-node scripts/seed.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "ts-node": "^10.9.2"
  }
}
