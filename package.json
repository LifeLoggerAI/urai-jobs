
{
  "name": "urai-jobs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/web\" \"npm run emulators\"",
    "build": "npm run build -w packages/web && npm run build -w packages/functions",
    "emulators": "firebase emulators:start",
    "seed": "npm run seed -w packages/functions",
    "lint": "npm run lint -w packages/web && npm run lint -w packages/functions"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
