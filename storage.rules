rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /resumes/{applicationId}/{filename} {
      allow read: if request.auth != null && exists(/databases/(default)/documents/admins/$(request.auth.uid));
      allow write: if request.auth != null; // Further validation in callable function
    }
  }
}
