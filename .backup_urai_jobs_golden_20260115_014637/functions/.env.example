# Safe defaults (emulator-friendly). In prod, set via Firebase env/config/Secrets.
ALLOW_ANON_ENQUEUE=false
JOB_LEASE_SECONDS=120
JOB_MAX_ATTEMPTS_DEFAULT=5
JOB_BACKOFF_BASE_SECONDS=5
JOB_BACKOFF_MAX_SECONDS=300
JOB_CONCURRENCY_DEFAULT=5
